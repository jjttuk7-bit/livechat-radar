# DESIGN.md — LiveChat Radar

**적용 시스템:** Linear — "midnight precision instrument"
**출처:** https://styles.refero.design/style/90ce5883-bb24-4466-93f7-801cd617b0d1
**구현:** `src/index.css`의 Tailwind 4 `@theme` 블록이 단일 출처다.

---

## 왜 Linear인가

이 제품은 정치·시사 라이브 채팅을 실시간으로 감시하는 **고밀도 다크 콘솔**이다.
한 화면에 패널 10개가 동시에 떠 있고, 색은 장식이 아니라 6개 분류축을 구분하는 정보다.

Linear이 맞는 이유:
- **다크 커맨드 센터** — near-black을 테마가 아니라 기반으로 쓴다. 우리 구조와 동일
- **태그·배지용 색조 팔레트 + 액션용 단일 액센트** — 6축 의미 색상 + 주 액션에 1:1 대응
- **그림자 없이 hairline 보더로 깊이** — 이미 우리가 하던 방식
- **장식 최소** — 데이터가 주인공인 화면에 맞는다

검토했으나 탈락한 후보 — **Factory**("terminal war room at midnight"):
분위기는 이 제품에 더 어울렸지만 핵심 규칙이 **"추가 액센트 색상 금지"**(Signal Orange + Metric Green만)였다.
색을 2개로 줄이면 6축 구분이 사라져 대시보드를 읽을 수 없다. 또 "밝은 카드가 유일한 밝은 객체"인
에디토리얼 구성은 패널 다수가 병렬로 뜨는 콘솔과 맞지 않는다.

---

## 색상

### 뉴트럴 (Linear 원본 그대로)

| 토큰 | 값 | 역할 |
|------|-----|------|
| `slate-950` | `#08090a` | Void — 페이지 캔버스 |
| `slate-900` | `#0f1011` | Carbon — 카드 표면, 내비 |
| `slate-850` | `#161718` | Obsidian — 상승 표면 |
| `slate-800` | `#23252a` | Graphite — 기본 보더 |
| `slate-700` | `#383b3f` | Smoke — 고대비 hairline |
| `slate-600` | `#62666d` | Ash — 약화 본문 |
| `slate-500` | `#8a8f98` | Fog — 3차 텍스트, placeholder |
| `slate-400` | `#9ba1a9` | (보간) 고밀도 라벨 가독성 |
| `slate-300` | `#d0d6e0` | Mist — 2차 제목, 버튼 텍스트 |
| `slate-200` | `#e5e5e6` | Bone — 본문 |
| `slate-50` | `#ffffff` | Paper — 최고 대비 |

### 6축 의미 색상

**각 축의 색은 정보다.** 임의로 바꾸면 화면의 의미가 바뀐다.

| 축 | 토큰 | 값 | Linear 원명 |
|----|------|-----|------------|
| `agenda` 아젠다·이슈 | `cyan-400` | `#02b8cc` | Signal Teal |
| `stance` 반응·의견 | `blue-400` | `#6366f1` | Iris Violet |
| `emotion` 정서·온도 | `amber-400` | `#f2b422` | **확장** (아래 참조) |
| `inquiry` 질문·요구 | `emerald-400` | `#27a644` | Pulse Green |
| `loyalty` 참여·후원 | `violet-400` | `#8b5cf6` | Lavender |
| `risk` 리스크·운영 | `rose-400` | `#eb5757` | Coral Red |

### 주 액션 — Acid Lime

`lime-400` `#e4f222`. **화면당 단일 주 액션에만** 쓴다. 축 색상으로는 절대 쓰지 않는다.

현재 적용: 연결 전에는 `연결`, 연결 후에는 `분석`이 주 액션이 된다 (동시에 둘이 lime이 되지 않는다).

---

## 의도적 이탈 3건

Linear 원본과 다른 부분을 숨기지 않고 기록한다. 이유가 사라지면 되돌려야 한다.

**1. `emotion` 축에 Amber `#f2b422` 추가**
6축을 구분하려면 6개 색조가 필요하다. Linear의 비액션 액센트는 Green·Coral·Teal·Iris·Lavender 5개이고,
Iris와 Lavender는 인접해 구분이 어렵다. 따뜻한 색조는 Coral뿐인데 Coral은 리스크에 써야 한다.
Amber는 이 앱에서 이미 `warning` 상태로 쓰이던 색이라 일관성도 유지된다.

**2. 타입 스케일이 Linear보다 훨씬 촘촘하다**
Linear 본문은 16px/1.5다. 이 앱은 9~13px을 쓴다. 마케팅 사이트가 아니라 **운영 콘솔**이고,
정보 밀도가 제품 요구사항이기 때문이다. Linear에서 가져온 것은 스케일이 아니라
**트래킹 규칙**(15px 이하 −0.011em), **굵기 규율**(700+ 금지), **OpenType 설정**이다.

**3. Berkeley Mono → JetBrains Mono**
Berkeley Mono는 유료 폰트다. 역할(숫자·ID·단축키)은 동일하게 유지한다.

---

## 타이포그래피

- **본문/UI:** Inter Variable 300–600. OpenType `cv01`, `ss03`, `zero` 활성
  (`zero`는 slashed zero — 숫자가 많은 콘솔에서 0/O 혼동을 없앤다)
- **수치/ID:** JetBrains Mono 400–500 + `tabular-nums`
  (실시간으로 갱신되는 숫자가 자리마다 흔들리면 읽을 수 없다)
- **트래킹:** 본문 −0.011em / h2·h3 −0.012em / h1 −0.022em
- **굵기:** 400 기본, 500·600은 강조에만. **700+ 금지** (Linear 규칙)

---

## 형태 · 간격 · 깊이

- **기본 단위** 4px
- **반경** 버튼 6px(`rounded-lg`) / 카드 12px(`rounded-xl`) / 배지 4px / 모달 16px
- **깊이** 그림자·글로우·블러를 쓰지 않는다. `border-slate-800` hairline과 표면 밝기 차이로만 만든다
- **모션** 0.15s, `cubic-bezier(0.4, 0, 0.2, 1)`. 스프링·패럴랙스 금지 (CLI 도구의 감각)

---

## 하지 말 것

- **700+ 굵기** — 위계는 색과 크기로 만든다
- **UI 컴포넌트에 그라디언트** — 로고 마크도 평면 표면 + 단색 아이콘이다
- **그림자로 카드 분리** — 보더를 쓴다
- **축 색상을 액션에 쓰거나, lime을 축에 쓰기** — 색이 의미를 잃는다
- **6축 외 새 색조 추가** — 추가하려면 이 문서에 이유를 남긴다
- **`slate-*` 같은 팔레트 이름을 새로 만들기** — `@theme`에서 값만 바꾼다.
  컴포넌트는 Tailwind 표준 클래스를 그대로 쓰므로 팔레트 교체가 한 파일에서 끝난다

---

## 안전 제약과의 관계

디자인은 `docs/plans/politics-pivot.md` 2절의 제약을 우회할 수 없다.

- 리스크·선거 신호 패널에는 면책 문구가 **상시 노출**된다 (D-5)
- 리스크 라벨은 단정하지 않는다 — "가짜뉴스"가 아니라 "검증 필요", "명예훼손"이 아니라 "명예훼손 소지" (D-4)
- 개인 카드에 정치성향을 표시하지 않는다. 비민감 축만 (D-1/D-2)
- 집계 차트에서 개인 목록으로 내려가는 인터랙션을 만들지 않는다 (D-3)
- 자동 삭제·차단 버튼을 두지 않는다 — 권고까지만

---

## 구현 메모

`src/index.css`의 `@theme`이 Tailwind 표준 색 이름(`slate-*`, `cyan-*` 등)의 **값만** 재정의한다.
따라서 컴포넌트는 손대지 않아도 팔레트가 바뀐다.

> ⚠️ CSS 주석 안에 `*/` 시퀀스를 만들지 마라. `slate-*/cyan-*`처럼 쓰면 주석이 조기 종료되어
> **뒤따르는 `@theme` 블록이 조용히 사라진다.** 빌드는 성공하고 색만 기본값으로 남으므로 알아채기 어렵다.
> 실제로 이 파일에서 한 번 발생했다.

검증 방법:
```bash
npm run build
grep -c "0f1011" dist/assets/*.css   # 1이어야 한다 (0이면 @theme이 누락됨)
```
